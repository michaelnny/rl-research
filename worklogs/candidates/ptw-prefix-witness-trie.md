status: parked-failed-implementation
last_run: 20260605-07-auto

The Prefix-Witness Trie (PTW) algorithm maintains a trie of executed action prefixes and for each node computes a coordinate-wise minimum vector over all terminal outcomes of trajectories sharing that prefix. The update weight at each (trajectory, prefix-depth) pair is the L1 norm of the strictly-positive coordinates of the staircase increment `Δ(τ,k) = w(τ_{0:k+1}) − w(τ_{0:k})`, which is guaranteed non-negative by construction (extending a prefix shrinks the comparison set, which can only raise the coordinate-wise min). The run completed cleanly (no crashes, no contract violations) but scored below random on both vector envs (Deep Sea Treasure 99.0 vs random 194.0; Resource Gathering 0.011 vs random 1.331). This matches the hypothesis's own predicted cold-start failure mode: on terminal-only or near-terminal-only vector channels the floor stays uniformly zero until the first success trajectory enters the buffer, silencing the staircase entirely. In 120s, exploration alone may not reach a success. The structural novelty is intact (Reviewer verdict: novel-direction); the mechanism is not a rebadge of advantage, return-to-go, VCC, FED, or PICAV. The fix for the next attempt is to pair PTW with a lightweight exploration bonus (count-based or epsilon-greedy escalation) active only until first success per env is recorded, or to switch from min-floor to a robust 10th-percentile floor to get non-zero signal from near-successful trajectories. Channel-wise MAD standardization before L1 remains required to prevent magnitude-dominated collapse.
