status: alive-promising
last_run: 20260606-09-auto
nearest_prior: attempt-15-FED (sprint-4 empirical-Pareto family)
side_information: [vector diagnostics, transition geometry]

FFTV computes the per-(state-hash, action, channel) IQR of empirical first-firing-time offsets T_m(tau, t) — the wait from each step until channel m next fires — and applies a Pareto-non-dominance logit nudge toward actions whose dispersion vector row is non-dominated in the partial order on R^k. Unlike the FED/CEC/CWTP/TPP sprint-4 family (which required observation-hash collisions or terminal-outcome matches to fire), FFTV fires from episode 1 on dense channels (step-penalty, validity) because every step contributes a T_m record. Evidence: DST score=1382 vs strong=285 (4.8x margin), RG score=1.331 = random=strong (no discrimination). The DST result is the strongest single-env margin seen in sprint 4. The RG collapse is consistent with the hypothesis's own predicted "terminal-only-channel collapse": both precious channels on RG (resources, goal) may fire only at terminal steps, leaving only the step-penalty channel with non-degenerate IQR and collapsing the Pareto vote to a single-channel maximization (curiosity rebadge). What is still needed: (1) per-channel IQR fill-rate instrumentation on RG to confirm the bootstrap failure diagnosis; (2) a warm-start or success-seeding mechanism to populate the precious-channel IQR on RG before the operator degenerates; (3) a core-stage run to check DoorKey/KeyCorridor performance. The structural distinction from GVFs/SF (#14) is well-articulated (dispersion of event timings, not expected cumulant magnitudes; Pareto-non-dominance, not any linear scalarization). FLAG: read this before the next Researcher proposal — this is the first sprint-4 alive-promising candidate.
