status: alive-weak
last_run: 20260605-02-auto
nearest_prior: 02-bric
side_information: [transition geometry, vector diagnostics]

SIT (Suffix-Inheritance Trie) maintains a prefix tree over executed action-sequences where each node stores the observation hash at that prefix and the multiset of outcome vectors from all suffixes continuing from it. The improvement operator grafts a Pareto-dominant suffix from a donor node to a receiver node whenever the two share an observation hash, then verifies the graft with a single rollout — committing if dominance replicates, marking non-fungible otherwise. The mechanism is structurally novel: graft points are on-distribution by construction (from observation-equivalence within the trie), distinguishing it from BRIC's off-distribution edit grammar, and it has no goal or event lens distinguishing it from ETB/HPC. The run completed without crash and scored beat_random=0, beat_strong=0 across all 4 envs (DoorKey-8x8 scored 0.0). The failure is a bootstrap problem: on long-horizon sparse envs the trie exploration policy (softmax over uniformly zero outcomes) reduces to random walk before any reward-bearing suffix exists, so the graft operator has nothing to operate on. Evidence still needed: a run with an explicit seeding phase (e.g. pure random rollouts loaded into the trie before grafting begins) to verify that once reward-bearing suffixes exist the graft operator actually improves outcomes and that observation-hash collision rate is manageable.
