status: alive-promising
last_run: 20260606-06-auto
nearest_prior: RND/curiosity (disqualifier), #14 Primal Behavior Flow Pivot
side_information: [learned dynamics, vector diagnostics]

CWAI (Channel-Wise Action Influence) trains a self-supervised forward model f_θ: (o_t, a_t) → o_{t+1} and at each decision step computes the per-(action, channel) Jacobian column-norm matrix G[a,m] = ‖∂f_θ(o_t,a)[m]/∂e(a)‖_2. The improvement operator nudges policy logits toward the Pareto-non-dominated rows of G (coordinate-wise dominance over channels, never scalarized). On Deep Sea Treasure (vector env), CWAI scored 1419.0 vs strong baseline 285.0 — a 4.97x margin — demonstrating that the per-channel Jacobian structure is non-trivial and action-differentiating even before reward is observed. Resource Gathering scored 0.011 (below random 1.331), consistent with the hypothesis's predicted failure mode: stochastic transitions shrink the Jacobian toward the noise floor. The structural distinction from RND/curiosity holds — CWAI uses local linearization of the forward map (nonzero even at zero prediction error), not error magnitude. Evidence still needed: (1) core-stage results (sparse + vector envs) to check generalization beyond vector envs; (2) logging of per-channel Jacobian column-norm distributions to verify the rank-1 collapse falsifier is not triggered on DST; (3) at least one additional env beating strong to reach the ≥ 2 env threshold.
