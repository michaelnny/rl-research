---
verdict: failed-structural
nearest_prior_or_disqualifier: atp-action-tangent-persistence (attempt-31), chx-cumulant-hull-extremality (attempt-17)
side_information: [vector diagnostics, policy-distribution-shape (internal)]
---

## Verdict reasoning

- **Structural distinction:** PRAR's indexing axis (policy-distribution-shape regime: entropy/top-prob/top-2-gap bins) and primitive (antisymmetric position-weighted within-episode channel residual) are genuinely not KTAC/DPC/any named disqualifier under variable renaming. The Pareto-non-dominance vote over F[r,a,:] is invariant to per-channel monotone rescaling, unlike any w^T r scalarization. The structural argument is sound. The failure is not a structural rebadge — it is the same substrate-induced collapse that took down ATP and CHX.
- **Substrate collapse to k_eff=1:** On DST and RG, the step-penalty channel has constant within-episode firing (`v_t[m] = -1` every step → `v_t[m] - μ_E[m] = 0`), zeroing that channel's F entries as predicted by failure mode (c). The only surviving channel is the terminal-only reward channel (k_eff = 1). With a single channel the Pareto-non-dominance count reduces to a scalar comparison on F[r,a,terminal]: the action-regime pair most often associated with terminal firing dominates. On DST, the nearby low-value treasure (rows 1-3) is visited far more often during exploration than deep high-value treasure, accumulating higher N[r,a] and higher F[r,a,terminal] magnitude — the operator steers toward nearby low-value termination, exactly as ATP (#31) did via persistence horizons. Scored 189/0.011 vs random 194/1.331 (below random on both vector envs, indicating active harm).
- **Primitive count:** One primitive (position-weighted residual tensor F) + one improvement operator (Pareto-non-dominance count logit nudge). Clean shape — the failure is not stack complexity.
- **Evidence quality:** 0 of 2 vector envs beat random. The hypothesis's own falsifier (both vector envs below random) is confirmed. The regime-as-replacement-for-state-cluster claim is falsified for this substrate.
- **Failure-mode informativeness:** The failure rules out the entire "antisymmetric within-episode-position residual with k_eff=1 terminal channel" sub-family. Any candidate that relies on the antisymmetric weight to separate terminal-only-channel signal will face the same collapse: with only one surviving channel, the Pareto vote degenerates to scalar comparison, and frequency bias toward nearby-terminal states drives below-random harm on DST-style envs. The regime indexing does not rescue this because even with many regimes populated, each regime's F slice still has k_eff=1 and the same scalar ordering bias.

## Lesson for the next iteration

PRAR extends the CHX/ATP/CRP family ruling: any primitive that reduces to k_eff=1 on the substrate's terminal-only vector channels — whether via hull geometry, rank position, persistence horizons, or antisymmetric position weighting — will produce scalar-channel behavior indistinguishable from "prefer the most frequently visited terminal state," which actively harms performance on DST-style multi-value envs; a viable candidate must either use a multi-channel signal that remains non-degenerate before the first rewarded trajectory, or include an explicit exploration primitive that ensures high-value terminal states are visited before the F/Pareto signal locks in.
