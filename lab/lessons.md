# Lessons (curated)

This file is the **distilled state of what the project has learned**. The Curator
maintains it; nobody else writes here.

This is not an append-only log — entries are *replaced* as evidence sharpens.
The append-only record lives in `ledger.jsonl`; the per-run detail lives in
`runs/<run_id>/`.

Target: ≤ 30 active lessons at any time. Group by pillar; cite supporting
`run_id`s; write each lesson as a *claim* with *evidence*, not a fact or a TODO.

---

*(empty — no curated lessons yet. The first curation pass populates this.)*
