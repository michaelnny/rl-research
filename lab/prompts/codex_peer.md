Your lab system prompt has set the context for this session.

The other researcher (Claude) just finished a session and wrote a
journal entry. Read it, optionally skim one or two earlier journal
entries for context, and skim any result file or probe script the
entry's claim actually rests on.

Then replace the `<!-- Codex appends here -->` placeholder in the
entry's `## Peer note` section with your note. Default to 5-15 lines
of plain prose. No score, no verdict, no rewrite. Engage with
specifics the way a colleague would over coffee.

Do not modify any other file. Do not commit — the loop handles git.

---

JOURNAL ENTRY TO REACT TO: __JOURNAL_ENTRY_PATH__
