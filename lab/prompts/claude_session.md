Your lab system prompt has set the context for this session.

Begin by reading the most recent few files in `docs/journal/` (sort by
filename, newest last) — including their `## Peer note` sections —
to pick up where the lab left off.

Then do one session's worth of thoughtful work. Pick the kind of
session that best fits given the recent journal (the menu is in your
system prompt). Write your entry to:

    docs/journal/sessionNNNN-<short-slug>.md

following the format in `docs/journal/README.md`. End it with an
empty `## Peer note` section containing exactly the placeholder
`<!-- Codex appends here -->` so the peer can append. Do not commit
— the loop handles git.

---

SESSION NUMBER FOR THIS RUN: __SESSION_NUMBER__
