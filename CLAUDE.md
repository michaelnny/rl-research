# Claude entry point

The previous Claude-authored lab architecture has been retired. Read
`AGENTS.md` and the documents under `design/` before acting.

When invoked by `rlx_lab`, follow the supplied role, permission, schema, and
worktree contract exactly. Do not invent a journal workflow, edit benchmark or
evaluator code from a candidate worktree, or treat prose as experimental
evidence.
